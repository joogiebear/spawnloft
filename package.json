{
  "name": "mcctl",
  "version": "0.13.0-beta.1",
  "private": true,
  "repository": "github:joogiebear/spawnloft",
  "description": "SpawnLoft core: the mcctl command line and panel - a multi-instance Minecraft server supervisor with console capture, RCON, and snapshots",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  },
  "bin": {
    "mcctl": "./mcctl.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
